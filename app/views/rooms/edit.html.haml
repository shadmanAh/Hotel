- content_for :title do
  Hotel
  \/
  Hotel
  = @hostel.name
  \/
  Editing room 
  = @room.title

= link_to 'Hotels', hostels_path
\/
= link_to @room.hostel.name, hostel_path(@room.hostel)
\/
= link_to @room.title, hostel_room_path(@hostel, @room)
\/
edit
%p
.card 
  .card-header
    %h1.d-flex
      .bi.bi-check2-square
      Editing room
  .card-body
    = render 'form'

= link_to 'Show room', hostel_room_path(@hostel,@room)
\|
= link_to 'Back to hotel', hostel_path(@room.hostel)
